1. 20260626 
    add watermark; 

2.  For PIP, consolidate cut, pip and watermark into one step, rather than cut main(with watermark), cut pip, make pip 3 steps. 
    meanwhile, formalize the code to gen ffmpeg command. 

3. Smooth function : do shift(-1) because it seems the switch is always a bit late. 

4. refactor the video_composer code to be more modulized . 
